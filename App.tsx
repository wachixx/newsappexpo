import { NativeBaseProvider, Box } from "native-base";

export default function App() {
  return (
      <NativeBaseProvider>
        <Box>Open up App.js to start working on your app!</Box>
      </NativeBaseProvider>
  );
}
